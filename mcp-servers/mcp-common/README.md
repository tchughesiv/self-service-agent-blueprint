# mcp-common

Shared Python helpers for MCP servers in this repository:

- **`mcp_common.headers`** — read HTTP headers from a FastMCP context (`header_first` for case-insensitive lookup).
- **`mcp_common.tracing`** — OpenTelemetry `trace_mcp_tool` decorator for MCP tool handlers.
